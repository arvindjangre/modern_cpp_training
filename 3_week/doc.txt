Parser

Use external libraries to create your class to handle the XML, JSON and CSV file.

Class should follow OOP's concept

User can read the file and see the content on console


install rapidxml lib
  $ sudo apt-get update
  $ sudo apt-get install rapidxml-dev

install rapidjson lib
  $ sudo apt-get update
  $ sudo apt-get install rapidjson-dev 